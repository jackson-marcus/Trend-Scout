services:
  api:
    build: .
    env_file: .env
    ports:
      - "8100:8000"
    volumes:
      - ./data:/app/data

  ui:
    build:
      context: .
      dockerfile: Dockerfile.ui
    environment:
      TRENDSCOUT_API_URL: http://api:8000
    ports:
      - "8601:8501"
    depends_on:
      - api
